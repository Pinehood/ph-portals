export enum PortalsRoutes {
  BASE = "/portals",
  PORTAL = "/:portal",
  ARTICLE = "/article/:portal/:articleId",
}

export enum ApiRoutes {
  BASE = "/api",
  PORTALS = "/portals",
  ARTICLES = "/portals/articles/:portal",
}
